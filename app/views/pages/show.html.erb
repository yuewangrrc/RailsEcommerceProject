<% content_for :title, "#{@page.title} - Swimi" %>

<div class="container py-5">
  <div class="row">
    <div class="col-lg-8 mx-auto">
      <h1 class="text-center mb-5"><%= @page.title %></h1>
      
      <div class="content">
        <%= raw @page.content %>
      </div>
    </div>
  </div>
</div>
